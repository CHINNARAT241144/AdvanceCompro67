function longestUnigueSubarrayWhithTargetSum(arr, TargetSum){
    let uniq_cha = [...new Set(arr)];
    let uniq = uniq_cha.join('');
    int_uniq = parseInt(uniq)

    // console.log(int_uniq)
    loop_round = int_uniq.len

    for (let round = 0;, round= < uniq.length;, round++ ){

    }





}

console.log(longestUnigueSubarrayWhithTargetSum([2,3,4,5,6,2,3,4,7],15));